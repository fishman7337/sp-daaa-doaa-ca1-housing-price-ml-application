# Scripts

Utility scripts live here. They should be deterministic, safe to run from the project root, and documented in the relevant runbook when they become part of a workflow.

`check_project.py` verifies the expected folder structure and warns about missing optional model artifacts.
