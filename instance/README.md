# Instance

Flask runtime files live here, including local SQLite databases and downloaded cache files.

This folder is for local/runtime state. Do not commit production databases, secrets, or generated cache contents.
